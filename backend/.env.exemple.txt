# Servidor
PORT=3001
NODE_ENV=development

# Frontend (CORS)
FRONTEND_URL=http://localhost:5173

# JWT
JWT_SECRET=seu-secret-super-seguro-aqui-mudar-em-producao

# Banco de Dados
# O SQLite usa arquivo local, sem necessidade de configuração adicional

# Puppeteer (opcional - para execução em servidor)
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=false
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser